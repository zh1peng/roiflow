% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pet_corr.R
\name{pet_corr_save}
\alias{pet_corr_save}
\title{Save PET Correlation Outputs}
\usage{
pet_corr_save(
  pet_corr_obj,
  out_dir = ".",
  out_prefix = "pet_corr_results",
  include_null = TRUE
)
}
\arguments{
\item{pet_corr_obj}{A \code{pet_corr_result} object.}

\item{out_dir}{Output directory.}

\item{out_prefix}{File prefix. Defaults to \code{"pet_corr_results"}.}

\item{include_null}{Logical. Save null vectors in RDS when available.}
}
\value{
Invisible named list of written file paths.
}
\description{
Saves \code{pet_corr()} outputs to disk (CSV + RDS). Side effects are
opt-in via this helper or \code{pet_corr(save = TRUE)}.
}
\examples{
data(pet_maps_dk68)
perm_id <- replicate(10, sample.int(nrow(pet_maps_dk68)), simplify = "matrix")
res <- pet_corr(
  brain = pet_maps_dk68[, "D1"],
  pet_select = "D2",
  perm_id = perm_id,
  n_perm = 10
)
tmp <- tempdir()
files <- pet_corr_save(res, out_dir = tmp)
}
