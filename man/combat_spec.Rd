% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combat_wrapper.R
\name{combat_spec}
\alias{combat_spec}
\title{Define ComBat Wrapper Specifications}
\usage{
combat_spec(
  roi_cols = NULL,
  roi_regex = "^(L_|R_)",
  batch_col = "Site",
  mod_formula = NULL,
  eb = TRUE,
  missing_fit = c("impute", "error"),
  single_batch = c("error", "noop"),
  keep_inputs = TRUE
)
}
\arguments{
\item{roi_cols}{Character vector. Explicit ROI column names. If \code{NULL},
columns are inferred via \code{roi_regex}.}

\item{roi_regex}{Character. Regex to identify ROI columns when \code{roi_cols}
is \code{NULL}. Default matches L_/R_ prefixed columns.}

\item{batch_col}{Character. Batch/site column name.}

\item{mod_formula}{Model formula for covariates (e.g., \code{~ Age + Sex}).
Use \code{NULL} for no covariates.}

\item{eb}{Logical. Use empirical Bayes shrinkage in ComBat.}

\item{missing_fit}{Character. How to handle missing values during fit:
\code{"impute"} (default) or \code{"error"} to stop if any NA is present
in ROI data.}

\item{single_batch}{Character. If only one batch level is present:
\code{"error"} (default) or \code{"noop"} to return a pass-through model.}

\item{keep_inputs}{Logical. If \code{TRUE}, return all columns on apply;
otherwise return only ROI columns.}
}
\value{
A list of specification values consumed by the wrapper functions.
}
\description{
Creates a specification list for the high-level ComBat data frame wrappers.
These settings control ROI selection, batch column, covariate formula, and
safety behaviors for missingness and single-batch data.
}
